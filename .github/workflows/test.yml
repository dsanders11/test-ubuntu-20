name: Test

on:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

jobs:
  test:
    name: Test Ubuntu latest
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: /bin/strip -h
