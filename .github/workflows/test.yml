name: Test

on:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

jobs:
  test:
    name: Test Ubuntu 20
    runs-on: ubuntu-20.04
    steps:
      - name: test
        run: ls /usr/lib/rpm/platform/
      - name: test 2
        run: cat /usr/lib/rpm/platform/armv7hl-linux/macros
